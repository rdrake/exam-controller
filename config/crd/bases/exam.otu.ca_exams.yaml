---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: exams.exam.otu.ca
spec:
  group: exam.otu.ca
  names:
    kind: Exam
    listKind: ExamList
    plural: exams
    singular: exam
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.schedule.unlock
      name: Unlock
      type: string
    - jsonPath: .status.computedLockTime
      name: Lock
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Exam is the Schema for the exams API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExamSpec defines the desired state of Exam.
            properties:
              email:
                description: ExamEmail configures email delivery.
                properties:
                  before:
                    default: 30m
                    type: string
                  from:
                    type: string
                  instructorEmail:
                    type: string
                  sendInterval:
                    default: 1s
                    type: string
                  subject:
                    type: string
                required:
                - from
                - instructorEmail
                - subject
                type: object
              schedule:
                description: ExamSchedule defines the timing for the exam lifecycle.
                properties:
                  dryRun:
                    description: ExamDryRunSpec configures the pre-exam smoke test
                      window.
                    properties:
                      before:
                        type: string
                    required:
                    - before
                    type: object
                  duration:
                    type: string
                  provisionBefore:
                    default: 1h
                    type: string
                  retention:
                    default: 24h
                    type: string
                  timeMultiplier:
                    default: 1.5
                    type: number
                  unlock:
                    format: date-time
                    type: string
                required:
                - duration
                - unlock
                type: object
              spares:
                type: integer
              students:
                items:
                  description: ExamStudent defines a student participating in the
                    exam.
                  properties:
                    email:
                      type: string
                    id:
                      type: string
                  required:
                  - email
                  - id
                  type: object
                type: array
              template:
                description: ExamTemplate defines the pod template for student instances.
                properties:
                  image:
                    type: string
                  port:
                    format: int32
                    type: integer
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                required:
                - image
                - port
                type: object
            required:
            - email
            - schedule
            - students
            - template
            type: object
          status:
            description: ExamStatus defines the observed state of Exam.
            properties:
              computedLockTime:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dryRun:
                description: DryRunStatus records the results of the dry run.
                properties:
                  completedAt:
                    format: date-time
                    type: string
                  failed:
                    type: integer
                  failures:
                    items:
                      description: DryRunFailure records a smoke test failure for
                        a student.
                      properties:
                        error:
                          type: string
                        student:
                          type: string
                      required:
                      - error
                      - student
                      type: object
                    type: array
                  passed:
                    type: integer
                required:
                - failed
                - passed
                type: object
              emailTime:
                format: date-time
                type: string
              message:
                type: string
              metrics:
                description: MetricsSummary provides quick counts for dashboard queries.
                properties:
                  emailsFailed:
                    type: integer
                  emailsSent:
                    type: integer
                  instancesFailed:
                    type: integer
                  instancesHealthy:
                    type: integer
                  totalSpares:
                    type: integer
                  totalStudents:
                    type: integer
                required:
                - emailsFailed
                - emailsSent
                - instancesFailed
                - instancesHealthy
                - totalSpares
                - totalStudents
                type: object
              phase:
                description: ExamPhase represents the current state of an Exam.
                type: string
              provisionTime:
                format: date-time
                type: string
              retentionDeadline:
                format: date-time
                type: string
              spares:
                items:
                  description: SpareStatus records the state of a spare instance.
                  properties:
                    phase:
                      description: StudentPhase represents the current state of a
                        student's instance.
                      type: string
                    slug:
                      type: string
                    url:
                      type: string
                  required:
                  - phase
                  - slug
                  type: object
                type: array
              students:
                items:
                  description: StudentStatus records the state of a single student's
                    resources.
                  properties:
                    emailSentAt:
                      format: date-time
                      type: string
                    emailStatus:
                      description: EmailStatus represents the email delivery state.
                      type: string
                    id:
                      type: string
                    phase:
                      description: StudentPhase represents the current state of a
                        student's instance.
                      type: string
                    slug:
                      type: string
                    url:
                      type: string
                  required:
                  - emailStatus
                  - id
                  - phase
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
