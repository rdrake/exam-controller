{{- if .Values.leaderElection.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "exam-controller.fullname" . }}-leader-election-rolebinding
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "exam-controller.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "exam-controller.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "exam-controller.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
